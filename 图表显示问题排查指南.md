# 图表显示问题排查指南

## 🔍 问题诊断

### 当前症状
- 数据生成成功（控制台显示 ✅ 演示数据已加载）
- 但图表不显示曲线
- 错误：`ChartModule: 图表未初始化`

### 根本原因
模块初始化顺序问题：
1. `detector-demo.js` 在 `DOMContentLoaded` 时立即初始化
2. 调用 `loadDemoData()` 时 `ChartModule` 还未完成初始化
3. 导致 `ChartModule.chart` 为 `null`

---

## ✅ 解决方案

### 方案 1: 测试独立页面（推荐用于验证）

打开以下任一页面测试图表功能：

```
http://localhost:3000/quick-test.html     - 最简单的测试
http://localhost:3000/simulation-test.html - 完整模拟测试
http://localhost:3000/chart-debug.html     - 调试工具
```

这些页面独立运行，不依赖复杂的模块加载顺序。

### 方案 2: 修复后的主页面

已修复 `detector-demo.js`：

**关键改动：**
1. 延迟 `loadDemoData()` 执行（1.5秒后）
2. 添加重试机制（检查 ChartModule 是否就绪）
3. 增强日志输出

**修改位置：**
- `initializeDemo()`: 延迟调用 `loadDemoData()`
- `loadDemoData()`: 添加就绪检查和重试
- `updateChartDirectly()`: 增强错误处理

---

## 🧪 测试步骤

### 1. 快速验证图表功能

```bash
# 确保服务器运行
.\start-system.ps1

# 打开浏览器访问
http://localhost:3000/quick-test.html
```

**预期结果：**
- 页面加载后自动显示 0-50mm 辐射曲线
- 两个明显的峰（25mm 和 35mm）
- 控制台无错误

### 2. 测试模拟数据生成器

```bash
http://localhost:3000/simulation-test.html
```

**功能：**
- 一键生成 95%/85%/70% 纯度曲线
- 实时统计（数据点、纯度、最大值）
- 数据表格展示
- CSV 导出

### 3. 调试主页面

```bash
http://localhost:3000/index.html
```

**观察控制台输出顺序：**
```
1. ChartModule 初始化
2. detector-demo 初始化
3. loadDemoData 延迟执行（1.5秒后）
4. 检查 ChartModule.chart 是否存在
5. 调用 ChartModule.updateChart()
6. 图表显示曲线
```

---

## 📊 控制台日志检查清单

### ✅ 正常日志顺序

```javascript
// 1. 模块加载
simulation-data-generator.js: 📊 模拟数据生成器已初始化
chart.js: 图表模块已加载
detector-demo.js: 放射化学纯度检测仪演示脚本已加载

// 2. 系统初始化
main.js: 🚀 系统启动中...
main.js: ✅ ChartModule 模块初始化完成
chart.js: 优化后的图表已创建  // ← 关键：图表实例已创建

// 3. 演示数据加载（延迟1.5秒后）
detector-demo.js: 📊 生成初始演示数据...
detector-demo.js: 🎯 准备更新图表: 501 个数据点
chart.js: 📊 ChartModule: 更新 501 个数据点
chart.js: ✅ ChartModule: 图表更新完成
```

### ❌ 错误日志（旧版本）

```javascript
detector-demo.js: 📊 生成初始演示数据...
chart.js: ⚠️ ChartModule: 图表未初始化  // ← 问题：太早调用
```

---

## 🔧 调试命令

### 在浏览器控制台手动测试

```javascript
// 1. 检查 ChartModule 状态
console.log('ChartModule:', window.ChartModule);
console.log('Chart实例:', window.ChartModule?.chart);

// 2. 检查模拟数据生成器
console.log('SimulationDataGenerator:', window.SimulationDataGenerator);

// 3. 生成测试数据
const testData = window.SimulationDataGenerator.generatePurityTestData(95);
console.log('测试数据:', testData);

// 4. 手动更新图表
if (window.ChartModule && window.ChartModule.chart) {
    const points = testData.points.map(p => ({ x: p.position, y: p.counts }));
    window.ChartModule.updateChart(points);
    console.log('✅ 图表已更新');
} else {
    console.error('❌ 图表未初始化');
}

// 5. 直接访问 Chart.js 实例
const canvas = document.getElementById('main-chart');
const existingChart = Chart.getChart(canvas);
console.log('Canvas上的图表:', existingChart);
```

---

## 🛠️ 常见问题

### Q1: 页面刷新后图表仍不显示

**解决：**
1. 硬刷新页面（Ctrl+Shift+R）
2. 清除浏览器缓存
3. 检查控制台是否有 JavaScript 错误

### Q2: 提示 "ChartModule: 图表未初始化"

**原因：** 模块加载顺序问题

**解决：**
1. 使用修复后的 `detector-demo.js`（已添加延迟和重试）
2. 或访问独立测试页面验证功能

### Q3: 数据生成但图表是空白的

**检查：**
```javascript
// 控制台执行
const chart = window.ChartModule.chart;
console.log('数据集:', chart.data.datasets[0].data);
console.log('数据点数:', chart.data.datasets[0].data.length);

// 如果数据存在但不显示，强制刷新
chart.update('active');
```

### Q4: 图表显示但曲线很小/看不清

**可能原因：** Y轴范围设置不当

**解决：**
```javascript
const chart = window.ChartModule.chart;
const data = chart.data.datasets[0].data;
const maxY = Math.max(...data.map(p => p.y));
chart.options.scales.y.max = maxY * 1.2;
chart.update();
```

---

## 📱 备用访问方法

如果主页面仍有问题，使用以下页面：

### 1. 快速测试页
```
http://localhost:3000/quick-test.html
```
- 最简化实现
- 自动运行测试
- 实时日志显示

### 2. 模拟测试页
```
http://localhost:3000/simulation-test.html
```
- 完整功能测试
- 多种纯度选择
- 数据导出功能

### 3. 调试工具页
```
http://localhost:3000/chart-debug.html
```
- 分步测试
- 性能监控
- 详细日志

### 4. 系统总览
```
http://localhost:3000/dashboard.html
```
- 功能导航中心
- 所有页面入口

---

## 🎯 验证修复

### 成功标志

1. **控制台日志：**
   - ✅ "ChartModule: 更新 501 个数据点"
   - ✅ "ChartModule: 图表更新完成"
   - 无 ⚠️ 警告

2. **页面显示：**
   - 看到平滑的曲线
   - 两个明显的峰（约 25mm 和 35mm）
   - X轴: 0-50mm
   - Y轴: 0-3500 cps

3. **交互测试：**
   - 鼠标悬停显示数值
   - 图例可点击
   - 可缩放和平移

---

## 📞 进一步帮助

如果问题仍然存在：

1. **截图提供：**
   - 浏览器控制台完整日志
   - 网络面板（F12 → Network）
   - 页面显示状态

2. **环境信息：**
   - 浏览器版本
   - 操作系统
   - 服务器运行状态

3. **测试结果：**
   - quick-test.html 是否正常？
   - simulation-test.html 是否正常？
   - chart-debug.html 显示什么？

---

**最后更新**: 2025-12-07  
**版本**: 2.1  
**修复内容**: 图表初始化顺序问题
