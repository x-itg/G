# 模拟数据演示使用指南

## 🎯 概述

系统现在可以在**没有真实硬件设备**的情况下运行完整的演示，使用智能模拟数据生成器展示所有功能。

## ✅ 已修复的问题

1. **✅ NULL 引用错误** - 所有 DOM 元素访问都添加了存在性检查
2. **✅ Chart.js 错误** - 画布元素缺失时优雅降级
3. **✅ Auth.js 错误** - 登录错误显示元素安全处理
4. **✅ 全局错误处理** - 改进了错误捕获和日志记录

## 🚀 快速开始

### 方式一：通过界面操作

1. **打开主页**: http://localhost:3000
2. **点击"开始扫描"按钮** （无需连接设备）
3. 系统将自动：
   - 生成 0-50mm 的模拟扫描数据
   - 应用数据处理算法
   - 显示实时动画效果
   - 分析峰值和纯度

### 方式二：使用控制台命令

打开浏览器控制台（F12），输入以下命令：

#### 1. 完整演示数据集
```javascript
runDemo()
```
生成包含多种场景的数据集：
- 标准扫描
- 高纯度样品 (95%)
- 中等纯度样品 (85%)
- 低纯度样品 (70%)
- 色谱分析数据

#### 2. 快速扫描
```javascript
quickScan(95)  // 95% 纯度
quickScan(85)  // 85% 纯度
quickScan(70)  // 70% 纯度
```

#### 3. 自定义数据生成
```javascript
// 生成自定义扫描数据
const data = SimulationDataGenerator.generateScanData({
    scanLength: 50,      // 扫描长度 (mm)
    stepSize: 0.1,       // 步长 (mm)
    peakCount: 3,        // 峰的数量
    peakHeight: 1200,    // 峰值高度
    noiseLevel: 0.1      // 噪声级别
});

console.log('生成的数据:', data);
```

#### 4. 应用处理算法
```javascript
const data = SimulationDataGenerator.generatePurityTestData(92);
const processed = SimulationDataGenerator.applyDataProcessing(data.points);

console.log('原始数据:', data);
console.log('处理结果:', processed);
console.log('检测到的峰:', processed.peaks);
console.log('纯度分析:', processed.purity);
```

## 📊 演示的算法功能

### 1. 数据平滑处理
- **算法**: 移动平均滤波
- **窗口大小**: 5点
- **效果**: 降低噪声，平滑曲线

### 2. 基线校正
- **方法**: 最小值基线扣除
- **作用**: 消除背景辐射影响

### 3. 峰值检测
- **算法**: 局部最大值检测
- **参数**: 阈值 = 最大值的 10%
- **输出**: 峰位置、高度、面积、宽度

### 4. 放射化学纯度计算
- **公式**: (主峰面积 / 总面积) × 100%
- **结果**: 百分比纯度值
- **详情**: 每个峰的百分比贡献

### 5. Rf 值计算
- **公式**: Rf = 峰位置 / 溶剂前沿
- **范围**: 0.000 - 1.000
- **精度**: 小数点后3位

## 🎨 数据可视化

系统将实时显示：
- ✅ **扫描曲线** - 位置 vs 计数率
- ✅ **平滑曲线** - 处理后的数据
- ✅ **峰标记** - 自动检测的峰位置
- ✅ **基线** - 背景辐射水平
- ✅ **实时数据** - 当前位置、计数率、进度

## 📈 示例输出

### 控制台输出示例：
```
📊 数据处理结果:
  - 原始数据点: 501
  - 平滑后数据点: 501
  - 检测到峰数: 2

  - 峰值信息:
    峰 1: 位置=35mm, 高度=1523, 面积=15234
    峰 2: 位置=20mm, 高度=234, 面积=1876

  - 放射化学纯度: 92.15%
  - 主峰位置: 35mm
```

### 纯度分析输出：
```javascript
{
  purity: 92.15,
  mainPeak: {
    position: 35,
    height: 1523,
    area: 15234,
    width: 4.2
  },
  totalPeaks: 2,
  peakDetails: [
    { peakNumber: 1, position: 35, percentage: "92.15" },
    { peakNumber: 2, position: 20, percentage: "7.85" }
  ]
}
```

## 🔬 模拟数据特性

### 真实性模拟：
- ✅ 高斯分布的峰形
- ✅ 泊松噪声模拟
- ✅ 基线漂移
- ✅ 多峰分离
- ✅ 真实的计数统计波动

### 可调参数：
- 扫描长度 (mm)
- 步长精度 (mm)
- 峰的数量和位置
- 峰高度和宽度
- 噪声级别
- 基线水平

## 🎯 测试场景

### 场景 1: 高纯度样品
```javascript
quickScan(95)
```
- 主峰纯度 > 95%
- 杂质峰很小
- 适合质量检测演示

### 场景 2: 中等纯度样品
```javascript
quickScan(85)
```
- 主峰纯度约 85%
- 有明显杂质峰
- 适合分离度演示

### 场景 3: 低纯度样品
```javascript
quickScan(70)
```
- 主峰纯度约 70%
- 多个杂质峰
- 适合复杂分析演示

### 场景 4: 色谱分析
```javascript
const data = SimulationDataGenerator.generateChromatographyData(
    [0.3, 0.5, 0.8],  // Rf 值
    50                 // 溶剂前沿
);
```

## 💡 提示和技巧

1. **实时演示**: 点击"开始扫描"自动运行动画演示
2. **快速测试**: 使用控制台命令 `quickScan()` 立即查看结果
3. **参数调整**: 修改生成参数观察算法对不同数据的处理
4. **批量测试**: 使用 `runDemo()` 生成多组测试数据
5. **算法验证**: 对比原始数据和处理后数据验证算法效果

## 🔧 技术细节

### 数据生成算法：
- **高斯峰**: `y = A * exp(-((x - μ) / σ)²)`
- **噪声**: 泊松分布 + 高斯白噪声
- **基线**: 常数 + 小幅度随机波动

### 处理算法：
- **平滑**: 移动平均
- **基线**: 最小值法
- **峰检测**: 一阶导数 + 阈值
- **纯度**: 面积归一化

## 📚 相关文档

- [系统主页](http://localhost:3000)
- [功能测试中心](http://localhost:3000/test-features.html)
- [检测日志](http://localhost:3000/detection-logs.html)
- [报告导出](http://localhost:3000/reports.html)

---

**提示**: 所有功能都可以在没有真实硬件的情况下完整演示！
