# 放射化学纯度检测仪 - 完整功能说明

## 🎯 系统概览

放射化学纯度检测仪验证系统是一个符合 CFR 21 Part 11 标准的硅胶板色谱扫描分析平台，提供完整的用户管理、审计追踪、数据分析和报告生成功能。

---

## 📱 页面导航

### 主要页面

| 页面 | 文件 | 功能说明 |
|------|------|----------|
| **系统总览** | `dashboard.html` | 所有功能的入口页面，功能导航中心 |
| **主控制台** | `index.html` | 检测主界面，实时监控和数据可视化 |
| **模拟测试** | `simulation-test.html` | 测试模拟数据生成，验证0-50mm曲线 |
| **账号管理** | `account-management.html` | 用户管理、权限控制、审计追踪 |
| **用户注册** | `register.html` | 新用户注册申请页面 |
| **检测日志** | `detection-logs.html` | 历史检测记录查询 |
| **报告导出** | `reports.html` | 生成和导出检测报告 |
| **设备管理** | `device-manager.html` | 设备连接和诊断 |
| **系统设置** | `system-settings.html` | 参数配置和系统维护 |

---

## ✨ 新增功能

### 1. 账号管理系统 (`account-management.html`)

完整的用户管理界面，包含4个主要功能选项卡：

#### 📊 用户列表
- 显示所有用户信息（ID、用户名、姓名、角色、权限级别、状态）
- 用户统计：总用户数、活跃用户、管理员数量
- 快速操作：添加、编辑、删除用户
- 角色标识：管理员(红)、操作员(橙)、查看者(蓝)

#### 🔐 权限管理
- **三级权限系统**：
  - **Level 1 - 查看者 (Viewer)**: 只能查看数据，无法修改
  - **Level 2 - 操作员 (Operator)**: 可执行检测、查看和导出数据
  - **Level 3 - 管理员 (Administrator)**: 完全访问权限，可管理用户和系统设置
- 权限矩阵显示各级别权限详情
- 符合 CFR 21 Part 11 三级权限要求

#### 📋 审计追踪
- 完整的操作记录（用户、时间、操作类型、详情）
- 按时间范围筛选审计日志
- 导出审计日志（CSV格式）
- 不可篡改的审计记录

#### ✍️ 电子签名
- 电子签名记录查询
- 签名验证和追溯
- 符合 CFR 21 Part 11 电子签名标准

### 2. 用户注册页面 (`register.html`)

自助注册功能，支持新用户申请账号：

#### 注册表单
- **必填信息**：
  - 用户名（3-20字符，实时验证重复）
  - 真实姓名
  - 邮箱地址（格式验证）
  - 密码（最少8字符，强度检测）
  - 确认密码（实时匹配验证）
  - 申请角色（查看者/操作员）
  
- **可选信息**：
  - 手机号码
  - 所属部门

#### 安全特性
- **密码强度检测**：
  - 弱：8字符以上
  - 中：包含大小写字母和数字
  - 强：包含特殊字符
  - 实时可视化强度指示器

- **实时验证**：
  - 用户名唯一性检查
  - 邮箱格式验证
  - 密码匹配验证
  - 表单完整性检查

- **用户协议**：必须同意条款才能注册

### 3. 模拟数据测试页面 (`simulation-test.html`)

独立的模拟数据生成和测试界面：

#### 功能特性
- **一键生成曲线**：
  - 95% 纯度曲线（高质量样品）
  - 85% 纯度曲线（中等质量）
  - 70% 纯度曲线（低质量样品）

- **实时统计**：
  - 数据点数（501个点，0.1mm步长）
  - 计算纯度
  - 最大计数率
  - 扫描时间

- **数据可视化**：
  - Chart.js 高质量曲线渲染
  - 平滑动画效果
  - 交互式工具提示
  - 响应式设计

- **数据管理**：
  - 数据表格展示
  - 峰位置标识
  - 导出CSV格式
  - 清空图表功能

### 4. 系统总览页面 (`dashboard.html`)

中央导航和功能展示页面：

#### 快速链接
- 8个主要功能页面的快速访问卡片
- 新功能标记
- 功能描述和图标

#### 合规性展示
- CFR 21 Part 11 合规特性说明
- 审计追踪能力
- 电子签名验证
- 数据完整性保护

#### 核心功能说明
- 8大功能模块详细介绍
- 技术架构说明
- 安全特性展示

---

## 🔬 核心功能模块

### 检测与扫描
- 0-50mm 扫描范围
- 0.1mm 位置精度
- 实时计数率监测
- 自动峰识别
- 模拟数据生成（无需硬件）

### 模拟数据生成器

**文件**: `assets/js/simulation-data-generator.js`

#### 核心算法
```javascript
// 生成指定纯度的测试数据
SimulationDataGenerator.generatePurityTestData(95)

// 快速生成扫描数据
SimulationDataGenerator.quickScan(95)
```

#### 功能特性
- **Gaussian 峰生成**: 模拟真实的辐射峰
- **Poisson 噪声**: 真实的计数统计波动
- **基线校正**: 自动背景扣除
- **峰检测**: 自动识别峰位置
- **纯度计算**: 基于峰积分的纯度测定

#### 数据处理
- 移动平均平滑
- 基线校正算法
- 峰识别和积分
- 纯度自动计算

### 用户管理系统

#### 三级权限控制
| 级别 | 角色 | 权限 |
|------|------|------|
| Level 1 | Viewer | 查看数据、查看报告 |
| Level 2 | Operator | 执行检测、导出数据、生成报告 |
| Level 3 | Administrator | 所有权限 + 用户管理 + 系统配置 |

#### 用户操作
- 添加新用户（模态框表单）
- 编辑用户信息
- 删除用户（需确认）
- 用户状态管理（启用/禁用）
- 角色分配

### 审计追踪系统

#### 记录内容
- 操作时间（精确到秒）
- 操作用户
- 操作类型（登录、检测、导出、修改设置等）
- 操作详情（IP地址、设备信息等）

#### 查询功能
- 按时间范围筛选
- 按用户筛选
- 按操作类型筛选
- 分页显示

#### 导出功能
- CSV 格式导出
- 包含所有字段
- 用于合规审查

### 报告生成系统

**文件**: `assets/js/report-exporter.js`

#### 支持格式
1. **CSV 原始数据**
   - 位置、计数率数据
   - Excel 兼容格式

2. **JSON 结构化数据**
   - 完整检测信息
   - 峰分析结果
   - 元数据

3. **PDF 专业报告**
   - 图表嵌入
   - 样品信息
   - 签名区域
   - 合规格式

4. **直接打印**
   - 调用浏览器打印对话框
   - 格式化输出

---

## 🛠️ 技术架构

### 前端技术栈
- **JavaScript**: 原生 ES6+ 模块化开发
- **Chart.js**: 数据可视化
- **HTML5/CSS3**: 响应式界面
- **ARIA**: 无障碍支持

### 后端架构
- **Express.js**: API 服务器（端口 3000）
- **SimplifiedDatabaseManager**: JSON 数据库
- **JWT**: 身份验证
- **RESTful API**: 标准化接口

### 数据库表
- `users`: 用户信息
- `detection_logs`: 检测记录
- `system_logs`: 系统日志
- `audit_trail`: 审计追踪
- `signatures`: 电子签名

### API 端点

#### 认证
```
POST   /api/auth/login       - 用户登录
POST   /api/auth/register    - 用户注册
POST   /api/auth/logout      - 退出登录
```

#### 用户管理
```
GET    /api/users            - 获取用户列表
POST   /api/users            - 创建用户
PUT    /api/users/:id        - 更新用户
DELETE /api/users/:id        - 删除用户
```

#### 日志
```
GET    /api/logs/system      - 获取系统日志
POST   /api/logs/system      - 记录系统日志
GET    /api/detection/logs   - 获取检测日志
```

#### 审计
```
GET    /api/audit/trail      - 获取审计记录
POST   /api/audit/log        - 记录审计操作
```

#### 报告
```
POST   /api/reports/export   - 导出报告
GET    /api/reports/:id      - 获取报告
```

---

## 🔐 安全与合规

### CFR 21 Part 11 合规
- ✅ 电子记录管理
- ✅ 电子签名验证
- ✅ 审计追踪
- ✅ 数据完整性
- ✅ 访问控制
- ✅ 安全性要求

### 安全特性
- 密码加密存储（bcrypt）
- JWT 令牌认证
- 会话超时管理
- CSP 内容安全策略
- SQL 注入防护
- XSS 防护

---

## 🚀 使用指南

### 快速开始

1. **启动系统**
   ```bash
   # 启动 API 服务器
   node api-server.js
   
   # 打开浏览器访问
   http://localhost:3000/dashboard.html
   ```

2. **登录系统**
   - 默认管理员: `admin` / `Admin123!`
   - 首次使用可通过注册页面创建账号

3. **开始检测**
   - 点击"主控制台"
   - 如无设备，系统自动加载模拟数据
   - 点击"开始检测"查看0-50mm扫描曲线

4. **测试模拟**
   - 访问"模拟测试"页面
   - 选择不同纯度生成曲线
   - 查看数据统计和详情

5. **管理用户**
   - 访问"账号管理"页面
   - 添加/编辑用户
   - 分配权限级别
   - 查看审计日志

### 页面访问路径

```
系统总览:      /dashboard.html
主控制台:      /index.html
模拟测试:      /simulation-test.html
账号管理:      /account-management.html
用户注册:      /register.html
检测日志:      /detection-logs.html
报告导出:      /reports.html
设备管理:      /device-manager.html
系统设置:      /system-settings.html
```

---

## 📊 数据流程

### 检测流程
1. 用户登录 → 身份验证
2. 连接设备 / 加载模拟数据
3. 配置检测参数
4. 开始扫描（0-50mm）
5. 实时数据可视化
6. 峰检测和纯度计算
7. 保存检测记录
8. 生成和导出报告
9. 记录审计日志

### 用户注册流程
1. 填写注册表单
2. 实时验证（用户名、邮箱、密码）
3. 提交注册信息
4. 后台创建用户记录
5. 发送确认（可选）
6. 管理员审批（可选）
7. 激活账号

---

## 🔧 模块说明

### JavaScript 模块

| 模块 | 文件 | 功能 |
|------|------|------|
| 错误处理 | `error-handler.js` | 全局错误捕获 |
| 工具函数 | `utils.js` | 通用工具方法 |
| 认证模块 | `auth.js` | 登录、权限验证 |
| 图表模块 | `chart.js` | Chart.js 封装 |
| 设备管理 | `device-manager.js` | 设备连接控制 |
| 检测日志 | `detection-logs.js` | 日志查询显示 |
| 报告导出 | `report-exporter.js` | 报告生成导出 |
| 模拟生成器 | `simulation-data-generator.js` | 模拟数据算法 |
| 主控制 | `detector-demo.js` | 检测主逻辑 |

---

## 📝 开发说明

### 添加新用户（代码示例）

```javascript
// 前端调用
const newUser = {
    username: 'operator2',
    fullname: '操作员王五',
    password: 'SecurePass123!',
    role: 'operator',
    email: 'wang@example.com'
};

fetch('http://localhost:3000/api/auth/register', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(newUser)
}).then(res => res.json())
  .then(data => console.log('用户创建成功:', data));
```

### 生成模拟数据（代码示例）

```javascript
// 生成95%纯度的测试数据
const data = SimulationDataGenerator.generatePurityTestData(95);

// 数据结构
{
    points: [
        { position: 0.0, counts: 120 },
        { position: 0.1, counts: 125 },
        // ... 501个数据点
    ],
    peaks: [
        { position: 25.3, height: 3500, area: 25000 }
    ],
    purity: 95.2,
    metadata: { ... }
}
```

### 记录审计日志（代码示例）

```javascript
// 记录用户操作
fetch('http://localhost:3000/api/audit/log', {
    method: 'POST',
    headers: { 
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify({
        action: '开始检测',
        details: '样品ID: S001',
        user: currentUser.username
    })
});
```

---

## 🎨 界面特性

### 响应式设计
- 适配桌面、平板、手机
- 弹性网格布局
- 触摸优化

### 无障碍支持
- ARIA 标签
- 键盘导航
- 屏幕阅读器支持
- 跳过链接

### 视觉设计
- 现代扁平化风格
- 渐变色主题（紫色系）
- 卡片式布局
- 平滑动画过渡

---

## 📞 支持与反馈

如有问题或建议，请查看：
- 系统日志: `/api/logs/system`
- 开发者控制台
- 错误日志文件

---

**版本**: 2.0  
**最后更新**: 2025-12-07  
**合规标准**: CFR 21 Part 11  
**技术支持**: 系统管理员
