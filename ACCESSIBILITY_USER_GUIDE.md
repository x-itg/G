# 辐射检测器可访问性功能使用指南

## 🎯 概述

辐射检测器系统已全面升级支持无障碍功能，符合 WCAG 2.1 AA 级标准。以下是各种可访问性功能的使用方法。

## ⌨️ 键盘导航

### 基础键盘操作
- **Tab**: 向前导航到下一个可聚焦元素
- **Shift + Tab**: 向后导航到上一个可聚焦元素
- **Enter**: 激活按钮或提交表单
- **空格键**: 激活按钮或切换复选框/单选按钮
- **Esc**: 关闭对话框、取消操作或返回上级菜单

### 系统快捷键
| 快捷键 | 功能 | 描述 |
|--------|------|------|
| `Ctrl + Alt + H` | 切换高对比度模式 | 在高对比度模式和正常模式间切换 |
| `Ctrl + Alt + +` | 增大字体 | 增加页面字体大小 |
| `Ctrl + Alt + -` | 减小字体 | 减少页面字体大小 |
| `Ctrl + M` | 切换移动端菜单 | 打开或关闭导航菜单 |
| `Ctrl + S` | 开始测量 | 启动数据采集 |
| `Ctrl + T` | 停止测量 | 停止当前测量 |
| `Ctrl + H` | 探头归零 | 将探头移动到起始位置 |
| `Ctrl + R` | 重置数据 | 清除所有测量数据 |
| `Ctrl + E` | 导出数据 | 导出测量数据 |
| `Ctrl + P` | 打印报告 | 打印当前报告 |
| `Ctrl + L` | 清理表单 | 清除登录表单内容 |

### 专用快捷键
| 快捷键 | 功能 | 描述 |
|--------|------|------|
| `Alt + C` | 检查Node.js | 检查系统Node.js环境（仅首页） |
| `Alt + I` | 查看说明 | 显示安装指南（仅首页） |
| `Ctrl + Enter` | 快速登录 | 在登录表单中快速提交 |

### 跳过链接
页面顶部提供跳过链接功能：
- 按 **Tab** 键显示跳过链接
- 按 **Enter** 键直接跳转到主要内容区域

## 🔊 屏幕阅读器支持

### 支持的屏幕阅读器
- **NVDA** (Windows)
- **JAWS** (Windows)
- **VoiceOver** (macOS/iOS)
- **TalkBack** (Android)
- **Orca** (Linux)

### 实时区域公告
系统会在以下情况提供语音公告：
- 页面加载完成
- 设备连接状态改变
- 测量开始/停止
- 错误消息显示
- 导出操作完成

### 表单辅助功能
- 所有输入字段都有明确标签
- 必填字段会语音提示
- 验证错误会实时语音播报
- 字段描述会通过屏幕阅读器朗读

## 🎨 视觉辅助功能

### 高对比度模式
启用方法：
1. 点击右上角的无障碍控制面板中的 **"高对比度"** 按钮
2. 或使用快捷键 **Ctrl + Alt + H**
3. 或在系统设置中启用高对比度主题

特点：
- 增强的边框和文本对比度
- 更明显的焦点指示器
- 优化的颜色方案
- 清晰的视觉层次

### 字体大小调节
控制按钮位于右上角无障碍面板：
- **A-**: 减小字体大小
- **A+**: 增大字体大小
- **重置**: 恢复到默认字体大小

字体大小选项：
- 正常 (100%)
- 大字体 (120%)
- 超大字体 (140%)

### 焦点指示器
- 所有可聚焦元素都有明显的焦点轮廓
- 高对比度模式下的焦点指示器更加醒目
- 支持键盘导航的用户可以看到清晰的焦点路径

## 📱 移动端可访问性

### 触摸友好设计
- 所有按钮最小尺寸 44×44 像素
- 足够的间距防止误触
- 手势操作支持滑动和双击

### 移动端导航
- 汉堡包菜单按钮位于屏幕左上角
- 底部滑出式导航面板
- 可折叠的结果面板
- 触摸友好的表单控件

### 屏幕阅读器移动端优化
- VoiceOver 手势支持
- TalkBack 导航优化
- 动态内容变更通知

## 🛠️ 无障碍控制面板

### 位置
屏幕右上角的无障碍控制面板提供快速访问功能。

### 功能说明

#### 高对比度切换
- **功能**: 启用或禁用高对比度模式
- **快捷键**: Ctrl + Alt + H
- **状态**: 按钮显示当前状态（按压/未按压）

#### 字体大小控制
- **A-**: 减小字体大小
- **A+**: 增大字体大小
- **快捷键**: Ctrl + Alt + + / -

#### 重置设置
- **功能**: 恢复所有可访问性设置为默认值
- **影响**: 清除高对比度模式、恢复默认字体大小

### 设置保存
- 所有设置会自动保存到浏览器本地存储
- 下次访问时自动恢复设置
- 设置在 7 天内保持有效

## 🔧 技术特性和兼容性

### 支持的浏览器
- **Chrome** 88+ (推荐)
- **Firefox** 85+
- **Safari** 14+
- **Edge** 88+

### 系统要求
- **操作系统**: Windows 10+, macOS 10.15+, Linux (现代版本)
- **屏幕分辨率**: 最低 1024×768，推荐 1920×1080 或更高
- **内存**: 建议 4GB 以上

### 无障碍标准
- **WCAG 2.1 AA** 级别合规
- **Section 508** 合规
- **EN 301 549** 合规

## 🐛 故障排除

### 常见问题

#### 键盘导航不工作
1. 确保页面已完全加载
2. 检查是否启用了键盘用户模式
3. 刷新页面重试
4. 检查浏览器兼容性

#### 屏幕阅读器无法读取内容
1. 确保屏幕阅读器已启动并正常运行
2. 检查系统可访问性设置
3. 确认浏览器支持 ARIA 标签
4. 尝试重新启动屏幕阅读器

#### 高对比度模式无效
1. 检查浏览器是否支持 CSS 媒体查询
2. 确认 JavaScript 已启用
3. 清除浏览器缓存后重试
4. 检查系统高对比度设置

#### 字体大小调节无效
1. 确认无障碍控制面板正常工作
2. 检查浏览器缩放设置
3. 刷新页面重试
4. 检查 JavaScript 是否启用

### 性能优化

#### 减少动画模式
系统会自动检测用户的动画偏好：
- **系统设置**: 操作系统中的"减少动画"设置
- **浏览器设置**: 浏览器中的"尊重网站运动偏好"设置

#### 内存使用优化
- 定期清理浏览器缓存
- 关闭不必要的浏览器标签页
- 使用现代浏览器以获得最佳性能

## 📞 技术支持

### 验证可访问性功能
在浏览器控制台中运行：
```javascript
// 运行完整验证
window.runAccessibilityValidation();

// 检查可访问性管理器
console.log(window.AccessibilityManager);

// 检查当前设置
console.log(window.AccessibilityManager.currentFontSize);
console.log(window.AccessibilityManager.isHighContrast);
```

### 调试信息
- 在控制台查看详细的可访问性日志
- 检查 ARIA 属性是否正确应用
- 验证键盘事件监听器是否正常工作

### 获取帮助
1. 检查浏览器控制台错误信息
2. 验证可访问性功能测试报告
3. 查看 `ACCESSIBILITY_ENHANCEMENT_REPORT.md` 了解详细技术信息

---

## 📝 更新日志

### v1.0.0 (2025-12-04)
- ✅ 完整的键盘导航支持
- ✅ 高对比度模式实现
- ✅ 屏幕阅读器全面支持
- ✅ 移动端可访问性优化
- ✅ 自动化可访问性测试
- ✅ 用户偏好保存功能
- ✅ 系统偏好自动检测

---

*本指南涵盖了辐射检测器系统的所有可访问性功能。如有任何问题或建议，请参考技术文档或联系开发团队。*