# 放射化学纯度检测仪系统部署指南

## 概述

放射化学纯度检测仪系统采用前后端分离架构，前端服务运行在3001端口，后端API服务运行在3000端口。系统通过串口与检测设备通讯，实现CFR 21 Part 11合规的放射化学纯度检测功能。

## 系统架构

### 整体架构
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端服务       │    │   后端服务       │    │   检测设备       │
│   Port: 3001    │◄──►│   Port: 3000    │◄──►│   串口通讯       │
│   Web界面       │    │   API接口       │    │   COM2/COM3     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 端口配置
- **前端服务**: http://localhost:3001
  - 用户界面
  - 系统验证页面
  - 性能监控界面
  - 权限管理界面

- **后端服务**: http://localhost:3000
  - API接口
  - 数据处理
  - 设备控制
  - 审计跟踪

- **串口设备**:
  - 控制端口: COM2 (9600, 8N1)
  - 数据端口: COM3 (9600, 8N1)

## 前置要求

### 软件环境
- **操作系统**: Windows 10/11 或 Windows Server 2016+
- **Node.js**: 16.0 或更高版本
- **npm**: 8.0 或更高版本
- **PowerShell**: 5.0 或更高版本
- **浏览器**: Chrome, Firefox, Edge (最新版本)

### 硬件要求
- **CPU**: 4核心或以上
- **内存**: 4GB RAM 或以上
- **存储**: 10GB 可用空间
- **串口**: USB转串口或板载串口
- **网络**: 本地网络连接

### 权限要求
- 管理员权限 (推荐)
- 串口访问权限
- 文件读写权限

## 部署步骤

### 步骤 1: 环境准备

#### 1.1 安装 Node.js
1. 访问 [Node.js官网](https://nodejs.org/)
2. 下载 LTS 版本 (16.0+)
3. 运行安装程序并按照向导完成安装
4. 验证安装:
```bash
node --version
npm --version
```

#### 1.2 安装串口驱动
1. 连接USB转串口设备
2. 设备管理器中确认串口正常工作
3. 记录串口号 (如 COM3, COM4)

#### 1.3 设置PowerShell执行策略
以管理员身份运行PowerShell:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 步骤 2: 获取系统文件

#### 2.1 从代码仓库克隆
```bash
git clone https://github.com/your-org/radiation-detector.git
cd radiation-detector
```

#### 2.2 或下载发布包
1. 下载最新发布版本压缩包
2. 解压到目标目录
3. 打开PowerShell并切换到项目目录

### 步骤 3: 配置系统

#### 3.1 端口配置 (config/port-config.json)
```json
{
  "ports": {
    "frontend": {
      "port": 3001,
      "description": "前端Web服务端口"
    },
    "backend": {
      "port": 3000,
      "description": "后端API服务端口"
    }
  }
}
```

#### 3.2 通讯配置 (config/communication-config.json)
```json
{
  "communication": {
    "protocol": "http",
    "cors": {
      "enabled": true,
      "origin": "http://localhost:3001"
    }
  }
}
```

#### 3.3 系统设置 (config/system-settings.json)
```json
{
  "communication": {
    "hardwareMode": "simulated",
    "serialPorts": {
      "comPort2": "COM2",
      "comPort3": "COM3",
      "baudRate": 9600,
      "dataBits": 8,
      "stopBits": 1,
      "parity": "none"
    }
  }
}
```

**重要**: 请根据实际设备连接情况修改串口号。

### 步骤 4: 安装依赖

#### 4.1 使用脚本安装 (推荐)
```powershell
.\start-system.ps1
```
脚本将自动:
- 检查Node.js环境
- 安装项目依赖
- 启动前后端服务
- 验证系统状态

#### 4.2 手动安装
```bash
npm install
```

### 步骤 5: 启动系统

#### 5.1 启动所有服务
```powershell
.\start-system.ps1
```

#### 5.2 验证启动
访问以下地址确认服务正常:
- 主页面: http://localhost:3001
- 验证页面: http://localhost:3001/system-verification.html
- API健康检查: http://localhost:3000/api/health

### 步骤 6: 系统验证

#### 6.1 运行验证脚本
```powershell
.\verify-system.ps1 -Detailed
```

#### 6.2 检查关键项目
- ✅ Node.js环境正常
- ✅ 依赖包完整安装
- ✅ 端口3000/3001可用
- ✅ API接口响应正常
- ✅ 前端页面可访问
- ✅ 串口配置正确
- ✅ 数据库连接正常

## 日常操作

### 启动系统
```powershell
.\start-system.ps1
```

### 停止系统
```powershell
.\stop-system.ps1
```

### 重启系统
```powershell
.\restart-system.ps1
```

### 系统验证
```powershell
.\verify-system.ps1 -Detailed -Export
```

### 查看日志
```powershell
# 查看启动日志
Get-Content logs\startup-$(Get-Date -Format 'yyyyMMdd').log -Wait

# 查看验证日志
Get-Content logs\verification-$(Get-Date -Format 'yyyyMMdd').log -Wait
```

## 故障排除

### 常见问题

#### 1. Node.js版本不满足要求
**现象**: 脚本提示Node.js版本过低
**解决**: 
1. 卸载旧版本Node.js
2. 从官网下载最新LTS版本
3. 重新安装并重启计算机

#### 2. 端口被占用
**现象**: 启动时提示端口3000或3001被占用
**解决**:
```powershell
# 查找占用端口的进程
netstat -ano | findstr :3000
netstat -ano | findstr :3001

# 终止占用进程
Stop-Process -Id <进程ID> -Force
```

#### 3. 依赖安装失败
**现象**: npm install 执行失败
**解决**:
```powershell
# 清除npm缓存
npm cache clean --force

# 删除node_modules重新安装
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
```

#### 4. 串口连接失败
**现象**: 串口设备无法连接
**解决**:
1. 检查串口设备是否正确连接
2. 验证串口驱动是否正常安装
3. 确认config/system-settings.json中的串口配置
4. 以管理员权限运行脚本

#### 5. 前端页面无法访问
**现象**: 浏览器无法打开 http://localhost:3001
**解决**:
1. 检查防火墙设置
2. 确认端口3001未被其他程序占用
3. 检查前端服务是否正常启动

### 日志分析

#### 启动日志分析
- `startup-YYYYMMDD.log`: 记录系统启动过程
- 查找关键词: ERROR, WARN, SUCCESS

#### 验证日志分析
- `verification-YYYYMMDD.log`: 记录系统验证过程
- 分析各个检查项目的状态

#### API日志
- 通过浏览器开发者工具查看Network选项卡
- 检查API请求的响应状态和内容

## 安全配置

### 网络安全
1. **防火墙配置**
   - 仅允许必要端口访问
   - 配置入站规则限制外部访问

2. **SSL证书** (生产环境)
   - 申请SSL证书
   - 配置HTTPS重定向

### 访问控制
1. **用户权限**
   - 设置强密码策略
   - 配置用户角色和权限

2. **API安全**
   - 启用API访问限制
   - 配置请求频率限制

### 数据保护
1. **数据库安全**
   - 定期备份数据库
   - 加密敏感数据

2. **审计日志**
   - 确保审计日志完整性
   - 配置日志保留策略

## 性能优化

### 系统性能
1. **内存优化**
   - 监控内存使用情况
   - 优化缓存配置

2. **CPU优化**
   - 关闭不必要的服务
   - 优化数据库查询

### 网络优化
1. **静态资源缓存**
   - 启用静态资源压缩
   - 配置浏览器缓存

2. **API优化**
   - 使用API缓存
   - 优化数据传输格式

## 维护计划

### 日常维护
- **每日**: 检查系统运行状态
- **每周**: 查看系统日志和性能指标
- **每月**: 备份重要数据和配置

### 定期更新
- **依赖更新**: 每月检查并更新npm依赖
- **安全更新**: 及时应用安全补丁
- **功能更新**: 根据需要更新系统功能

### 备份策略
1. **配置文件备份**
   - config目录下的所有文件
   - 数据库文件
   - 日志文件

2. **代码备份**
   - 完整项目代码
   - 版本历史记录

## 脚本使用详解

### start-system.ps1
**功能**: 一键启动系统
**参数**:
- `-Force`: 强制启动，忽略端口占用警告
- `-Verbose`: 显示详细输出

**使用示例**:
```powershell
.\start-system.ps1 -Force -Verbose
```

### stop-system.ps1
**功能**: 安全停止所有服务
**参数**:
- `-Force`: 强制停止所有进程
- `-Verbose`: 显示详细输出

**使用示例**:
```powershell
.\stop-system.ps1 -Force
```

### verify-system.ps1
**功能**: 全面验证系统状态
**参数**:
- `-Detailed`: 显示详细信息
- `-Fix`: 自动修复发现的问题
- `-Export`: 导出验证报告
- `-OutputPath`: 指定报告输出路径

**使用示例**:
```powershell
.\verify-system.ps1 -Detailed -Export
```

### restart-system.ps1
**功能**: 重启系统服务
**参数**:
- `-Force`: 强制重启
- `-Verbose`: 显示详细输出

**使用示例**:
```powershell
.\restart-system.ps1 -Verbose
```

## 技术支持

### 联系信息
- **技术支持**: tech-support@example.com
- **用户手册**: docs/用户使用指南.md
- **API文档**: http://localhost:3000/api/docs

### 问题反馈
1. 收集详细的错误信息和日志
2. 描述问题复现步骤
3. 提供系统环境信息
4. 发送至技术支持邮箱

### 文档资源
- 用户使用指南: `docs/用户使用指南.md`
- 系统维护指南: `docs/系统维护指南.md`
- 性能监控指南: `docs/性能监控使用方法.md`
- 权限操作指南: `docs/三级权限操作说明.md`
- 审计日志指南: `docs/审计日志查询方法.md`

---

**版本**: 2.0.0  
**更新时间**: 2025-12-06  
**作者**: xitg